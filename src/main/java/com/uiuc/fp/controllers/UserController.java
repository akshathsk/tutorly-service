package com.uiuc.fp.controllers;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class UserController {

  /*TODO:

    Things to work on -
    1. Check for free SMTP server
    2. Code the email module
    3. Create a script to load dummy data
    4. Create a trigger to update the overall ratings of a user when a user adds a rating
    5. Figure out how spring jpa can return data sorted by an attribute by default (example : topics returned should always be sorted by ratings and slot history by date desc)
    6. Check if FK validation is working by default. If not, figure out how to add validation and not throw DB errors to the UI.
    7. GET Tags - move topics to tags
    8. Check all domain model files again and check for the FK mappings

   */

}